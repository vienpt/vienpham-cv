/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export', // Outputs a Single-Page application (SPA)
  distDir: './dist', // Changes the output directory
}

export default nextConfig